name: CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  test:
    name: Run Tests
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'gradle'
          
      - name: Grant execute permission for gradlew
        run: chmod +x gradlew
        shell: bash
        
      - name: Run tests
        run: ./gradlew test --no-daemon
        
      - name: Generate coverage report and verify
        run: ./gradlew jacocoTestReport --no-daemon
        
      - name: Print coverage summary
        if: always()
        run: ./gradlew jacocoCoverageSummary --no-daemon
        continue-on-error: true
        
      - name: Run code quality checks
        run: ./gradlew codeQualityCheck --no-daemon
        continue-on-error: true
        
      - name: Run OWASP dependency check (non-blocking)
        run: ./gradlew dependencyCheckAnalyze --no-daemon
        continue-on-error: true
        
      - name: Verify code quality (fail on high-priority bugs)
        run: ./gradlew codeQualityVerify --no-daemon
        
      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: |
            build/test-results/test/**/*.xml
            build/reports/tests/test/**
          retention-days: 30
          
      - name: Upload coverage reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-reports
          path: |
            build/reports/jacoco/**
          retention-days: 30
          
      - name: Upload code quality reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: code-quality-reports
          path: |
            build/reports/spotbugs/**
            build/reports/pmd/**
            build/reports/checkstyle/**
            build/reports/dependency-check-report.html
            build/reports/dependency-check-report.json
          retention-days: 30

